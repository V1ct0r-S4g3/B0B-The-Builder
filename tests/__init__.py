"""Test package for the SC2 bot."""

# This file makes the tests directory a Python package, allowing relative imports.
# It also ensures that the src directory is in the Python path when running tests.
